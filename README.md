Its a pretty simple site that i made for university(practice). 
It uses firebase for its backend(authentification and database) and React.js with TailwindCSS for the frontend. 


To do: add stripe payment(the testing kind).
